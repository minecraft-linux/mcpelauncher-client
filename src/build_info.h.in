#pragma once

#include <cstddef>

#define MANIFEST_GIT_COMMIT_HASH "${MANIFEST_GIT_COMMIT_HASH}"
#define CLIENT_GIT_COMMIT_HASH "${CLIENT_GIT_COMMIT_HASH}"

#define SOURCE_BASELINE_ROOT_GIT_COMMIT_HASH "${SOURCE_BASELINE_ROOT_GIT_COMMIT_HASH}"
#define SOURCE_BASELINE_ROOT_GIT_DESCRIBE "${SOURCE_BASELINE_ROOT_GIT_DESCRIBE}"
#define SOURCE_BASELINE_ROOT_VERSION "${SOURCE_BASELINE_ROOT_VERSION}"
#define SOURCE_BASELINE_ROOT_GIT_WORKTREE_DIRTY "${SOURCE_BASELINE_ROOT_GIT_WORKTREE_DIRTY}"

#define MANIFEST_GIT_COMMIT_HASH_FULL "${MANIFEST_GIT_COMMIT_HASH_FULL}"
#define MANIFEST_GIT_DESCRIBE "${MANIFEST_GIT_DESCRIBE}"
#define MANIFEST_VERSION "${MANIFEST_VERSION}"
#define MANIFEST_GIT_WORKTREE_DIRTY "${MANIFEST_GIT_WORKTREE_DIRTY}"

#define CLIENT_GIT_COMMIT_HASH_FULL "${CLIENT_GIT_COMMIT_HASH_FULL}"
#define CLIENT_GIT_DESCRIBE "${CLIENT_GIT_DESCRIBE}"
#define CLIENT_VERSION "${CLIENT_VERSION}"
#define CLIENT_GIT_WORKTREE_DIRTY "${CLIENT_GIT_WORKTREE_DIRTY}"

#define BUILD_CMAKE_VERSION "${CMAKE_VERSION}"
#define BUILD_CMAKE_GENERATOR "${CMAKE_GENERATOR}"
#define BUILD_GENERATOR_TOOL_VERSION "${GENERATOR_TOOL_VERSION}"
#define BUILD_CXX_COMPILER_ID "${CMAKE_CXX_COMPILER_ID}"
#define BUILD_CXX_COMPILER_VERSION "${CMAKE_CXX_COMPILER_VERSION}"
#define BUILD_CXX_STANDARD "${CMAKE_CXX_STANDARD}"
#define BUILD_TARGET_SYSTEM "${CMAKE_SYSTEM_NAME}"
#define BUILD_TARGET_SYSTEM_VERSION "${CMAKE_SYSTEM_VERSION}"
#define BUILD_TARGET_PROCESSOR "${CMAKE_SYSTEM_PROCESSOR}"
#define BUILD_OSX_ARCHITECTURES "${CMAKE_OSX_ARCHITECTURES}"
#define BUILD_OSX_DEPLOYMENT_TARGET "${CMAKE_OSX_DEPLOYMENT_TARGET}"
#define BUILD_XCODE_VERSION "${XCODE_VERSION}"
#define BUILD_XCODE_BUILD_VERSION "${XCODE_BUILD_VERSION}"
#define BUILD_SDK_NAME "${SDK_NAME}"
#define BUILD_SDK_VERSION "${SDK_VERSION}"
#define BUILD_SDK_BUILD_VERSION "${SDK_BUILD_VERSION}"

#define BUILD_OPTION_BUILD_CLIENT "${BUILD_CLIENT}"
#define BUILD_OPTION_BUILD_UI "${BUILD_UI}"
#define BUILD_OPTION_BUILD_WEBVIEW "${BUILD_WEBVIEW}"
#define BUILD_OPTION_BUILD_TESTING "${BUILD_TESTING}"
#define BUILD_OPTION_USE_GAMECONTROLLERDB "${USE_GAMECONTROLLERDB}"
#define BUILD_OPTION_USE_OWN_CURL "${USE_OWN_CURL}"
#define BUILD_OPTION_USE_SDL3_AUDIO "${USE_SDL3_AUDIO}"
#define BUILD_OPTION_NO_OPENSSL "${NO_OPENSSL}"
#define BUILD_OPTION_GAMEWINDOW_SYSTEM "${GAMEWINDOW_SYSTEM}"

#define BUILD_GLFW_SOURCE_REVISION "${GLFW_SOURCE_REVISION}"

struct BuildDependencyRevision {
    const char* name;
    const char* gitCommit;
    const char* pinnedGitCommit;
    const char* worktreeDirty;
};

inline constexpr BuildDependencyRevision BUILD_DEPENDENCY_REVISIONS[] = {
${MANIFEST_DEPENDENCY_REVISION_ENTRIES}    {nullptr, nullptr, nullptr, nullptr}
};
